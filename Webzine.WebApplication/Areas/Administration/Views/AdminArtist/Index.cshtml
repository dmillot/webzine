@model Webzine.WebApplication.Areas.Administration.ViewModels.AdminArtistViewModel;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.css">
    <title>Document</title>
</head>

<body>
    <div class="container-fluid">
        <h1 class="mt-2">Artistes</h1>

        <hr />

        <a href="@Url.Action("Create", "AdminArtist")" class="btn btn-primary"><i class="fas fa-plus"></i> Ajouter un nouvel artiste</a>

        <table class="table table-striped table-bordered mt-3">
            <thead class="thead-light">
                <tr>
                    <th scope="col">Nom de l'artiste</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.artists)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td class="text-center">
                            <a asp-area="administration" asp-controller="artiste" asp-action="edit" asp-route-id="@item.Id" class="mr-1"><i class="fas fa-edit"></i></a>
                            <a asp-area="administration" asp-controller="artiste" asp-action="delete" asp-route-id="@item.Id" class="mr-1"><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js"
            integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
</body>

</html>